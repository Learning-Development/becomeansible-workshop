name: deploy ansible

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    step:
    - uses: actions.checkout@v1
    - uses: ./.github/action/ansible
